{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "npx prisma migrate resolve --rolled-back 20260621071018_add_message_read_delivered_at || true && npx prisma migrate resolve --rolled-back 20260621181926_add_token_blacklist || true && npx prisma migrate deploy && node dist/src/main.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
